[project]
name = "timeplus-recipes"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "confluent-kafka>=2.14.2",
    "grpcio-tools>=1.81.0",
    "protobuf>=6.33.6",
    "proton-driver>=0.2.13",
]
